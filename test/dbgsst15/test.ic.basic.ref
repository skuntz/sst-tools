debug probe demo configuration:
	 probeStartCycle  =  0
	 probeEndCycle  =  0
	 probeBufferSize  =  16
	 probePostDelay  =  8
	 probePort  =  0
	 verbose  =  1
	 cliControl  =  0
DbgSST15[cp0:DbgSST15:0]: numPorts=1
DbgSST15[cp0:DbgSST15:0]: minData=1
DbgSST15[cp0:DbgSST15:0]: maxData=100
DbgSST15[cp0:DbgSST15:0]: clockDelay=100
DbgSST15[cp0:DbgSST15:0]: clocks=10000
DbgSST15[cp0:DbgSST15:0]: traceMode=2
DbgSST15[cp0:DbgSST15:0]: tracing RECV events
DbgSST15[cp1:DbgSST15:0]: numPorts=1
DbgSST15[cp1:DbgSST15:0]: minData=1
DbgSST15[cp1:DbgSST15:0]: maxData=100
DbgSST15[cp1:DbgSST15:0]: clockDelay=100
DbgSST15[cp1:DbgSST15:0]: clocks=10000
DbgSST15[cp1:DbgSST15:0]: traceMode=1
DbgSST15[cp1:DbgSST15:0]: tracing SEND events
DbgSST15[cp1:ProbeControl:0]: probeMode=1
DbgSST15[cp1:ProbeControl:0]: probeStartCycle=0
DbgSST15[cp1:ProbeControl:0]: probeEndCycle=0
DbgSST15[cp1:ProbeControl:0]: probeBufferSize=16
DbgSST15[cp1:ProbeControl:0]: probePort=0
DbgSST15[cp1:ProbeControl:0]: probePostDelay=8
DbgSST15[cp1:ProbeControl:0]: cliControl=0
0 
Entering interactive mode at time 0 
Interactive start at 0
> > cp0 (SSTDEBUG::DbgSST15::DbgSST15)
> *probe_/ (SSTDEBUG::DbgSST15::DbgSST15_Probe)
cliType = 0 (unsigned int)
clockDelay = 100 (unsigned long)
clockHandler/ (SST::SSTHandler2<bool, unsigned long, SSTDEBUG::DbgSST15::DbgSST15, void, &SSTDEBUG::DbgSST15::DbgSST15::clockTick>)
clocks = 10000 (unsigned long)
component_state_ = 3 (SST::BaseComponent::ComponentState)
curCycle = 0 (unsigned long)
linkHandlers/ (std::vector<SST::Link*, std::allocator<SST::Link*> >)
maxData = 100 (unsigned long)
mersenne/ (SST::RNG::MersenneRNG)
minData = 1 (unsigned long)
my_info_/ ()
my_info_/ (SST::ComponentInfo*)
numPorts = 1 (unsigned int)
rCheck = 0 (long)
size = 0 (unsigned long)
traceMode = 2 (unsigned int)
v_double = 2.000000 (double)
v_false = 0 (bool)
v_float = 1.000000 (float)
v_true = 1 (bool)
> > Current watch points:
0: ALL : cp0/size > 90  : interactive
> WP0: ALL : cp0/size > 90  : interactive
>   Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
DbgSST15[cp0:sendData:100000]: data.size = 84
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
DbgSST15[cp1:sendData:100000]: data.size = 87
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
DbgSST15[cp0:sendData:200000]: data.size = 78
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
DbgSST15[cp1:sendData:200000]: data.size = 16
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
  Before Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> false
    No trace buffer
DbgSST15[cp0:sendData:300000]: data.size = 94
  After Clock Handler
    WatchPoint cp0/size tests:
      cp0/size > 90  -> true
    No trace buffer
    SetInteractive
DbgSST15[cp1:sendData:300000]: data.size = 36
Entering interactive mode at time 300000 
Watch point cp0/size buffer
> > > DbgSST15[cp0:sendData:400000]: data.size = 57
DbgSST15[cp1:sendData:400000]: data.size = 93
DbgSST15[cp0:sendData:500000]: data.size = 71
DbgSST15[cp1:sendData:500000]: data.size = 22
DbgSST15[cp0:sendData:600000]: data.size = 43
DbgSST15[cp1:sendData:600000]: data.size = 28
DbgSST15[cp0:sendData:700000]: data.size = 7
DbgSST15[cp1:sendData:700000]: data.size = 60
DbgSST15[cp0:sendData:800000]: data.size = 97
DbgSST15[cp1:sendData:800000]: data.size = 27
DbgSST15[cp0:sendData:900000]: data.size = 44
DbgSST15[cp1:sendData:900000]: data.size = 27
Entering interactive mode at time 1000000 
Running clock 1000000 sim cycles
> minData = 2 (unsigned long)
> current time = 1000000
> DbgSST15[cp0:sendData:1000000]: data.size = 81
DbgSST15[cp1:sendData:1000000]: data.size = 37
DbgSST15[cp0:sendData:1100000]: data.size = 100
DbgSST15[cp1:sendData:1100000]: data.size = 69
DbgSST15[cp1:handleEvent:1100000]: size = 84
DbgSST15[cp0:handleEvent:1100000]: size = 87
DbgSST15[cp0:sendData:1200000]: data.size = 62
DbgSST15[cp1:sendData:1200000]: data.size = 30
DbgSST15[cp1:handleEvent:1200000]: size = 78
DbgSST15[cp0:handleEvent:1200000]: size = 16
Entering interactive mode at time 1300000 
Running clock 1000000 sim cycles
> Exiting ObjectExplorer and shutting down simulation
Simulation is complete, simulated time: 1.3 us
